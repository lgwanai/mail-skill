# ✍️ 邮件签名 (Email Signatures)

> **🤖 给 AI Agent 的核心指令**
> 1. 这个文件专门用来存储用户的邮件签名。
> 2. 当你需要发送 (`send`) 或回复 (`reply`) 邮件时，**必须**首先读取此文件的内容。
> 3. 如果文件中有签名内容，请将其自动追加到你要发送的邮件正文末尾（除非用户明确说明不使用签名）。
> 4. 如果用户要求更新、修改或设置新的邮件签名，请使用文件编辑工具直接修改本 `references/SIGNATURE.md` 文件。

---

(空)