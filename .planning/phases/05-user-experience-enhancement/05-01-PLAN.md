---
phase: 05-user-experience-enhancement
plan: 01
type: execute
wave: 1
depends_on: ["05-00"]
files_modified: [scripts/mail_manager/templates.py, tests/test_templates.py]
autonomous: true
requirements: [TMPL-01, TMPL-02, TMPL-05]
user_setup: []
must_haves:
  truths:
    - "User can load a YAML template file from account directory"
    - "Template variables are extracted and validated"
    - "Missing required variables are detected before sending"
  artifacts:
    - path: "scripts/mail_manager/templates.py"
      provides: "Template loading and variable substitution"
      exports: ["Template", "TemplateManager", "load_template", "validate_variables"]
    - path: "tests/test_templates.py"
      provides: "Test coverage for template module"
      min_lines: 100
  key_links:
    - from: "TemplateManager"
      to: "YAML file"
      via: "ruamel.yaml or PyYAML"
      pattern: "yaml\\.safe_load"
---

<objective>
Implement YAML-based reply templates with variable placeholders and validation.

Purpose: Enable users to define reusable reply templates with {{variable}} placeholders that are validated before sending.
Output: Template module with loading, variable extraction, and validation.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-smart-classification/04-01-SUMMARY.md

## Existing Patterns

From `scripts/mail_manager/rules.py` - YAML loading pattern:
```python
import yaml
from pathlib import Path

def load_rules(rules_path: str | None) -> list[Rule]:
    if rules_path and Path(rules_path).exists():
        with open(rules_path) as f:
            data = yaml.safe_load(f)
```

From `scripts/mail_manager/models.py` - dataclass pattern:
```python
@dataclass
class Rule:
    name: str
    rule_type: str
    patterns: list[str]
    importance: str | None = None
    weight: float = 1.0
```

## Template Storage Location

Per `scripts/mail_cli.py:_get_account_paths`:
```
mail_data/{account_email}/templates/
```

## Variable Placeholders

Use Jinja2-style syntax: `{{sender_name}}`, `{{date}}`
Required variables: sender_name, date, subject (can be configured per template)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Template dataclass and variable extraction</name>
  <files>scripts/mail_manager/templates.py, tests/test_templates.py</files>
  <behavior>
    - Test 1: Template dataclass holds name, content, required_vars
    - Test 2: extract_variables finds {{var}} patterns in template content
    - Test 3: extract_variables returns empty list for no variables
    - Test 4: extract_variables handles multiple variables
  </behavior>
  <action>
    Implement in scripts/mail_manager/templates.py:
    1. Template dataclass with fields: name, content, required_vars (list[str])
    2. extract_variables(content: str) -> list[str] function
       - Use regex: r'\{\{(\w+)\}\}' to find {{variable}} patterns
       - Return unique variable names in order of appearance
    3. Add type annotations following project patterns
  </action>
  <verify>
    <automated>python -m pytest tests/test_templates.py::test_extract_variables -v</automated>
  </verify>
  <done>Template dataclass and extract_variables function work, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement template loading from YAML files</name>
  <files>scripts/mail_manager/templates.py, tests/test_templates.py</files>
  <behavior>
    - Test 1: load_template reads YAML file and returns Template
    - Test 2: load_template raises FileNotFoundError for missing file
    - Test 3: load_template raises ValueError for invalid YAML
    - Test 4: YAML format supports name, content, required_vars fields
  </behavior>
  <action>
    Implement in scripts/mail_manager/templates.py:
    1. load_template(path: str) -> Template function
       - Read YAML file using yaml.safe_load
       - Validate required fields (name, content)
       - Extract variables if required_vars not specified
       - Return Template instance
    2. Define YAML format:
       ```yaml
       name: "Thank You Reply"
       content: |
         Hi {{sender_name}},

         Thank you for your email dated {{date}}.
         Best regards.
       required_vars: ["sender_name", "date"]  # optional
       ```
    3. Add error handling with descriptive messages
  </action>
  <verify>
    <automated>python -m pytest tests/test_templates.py::test_load_template -v</automated>
  </verify>
  <done>load_template function works with YAML files, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement TemplateManager for listing and creating templates</name>
  <files>scripts/mail_manager/templates.py, tests/test_templates.py</files>
  <behavior>
    - Test 1: TemplateManager.list_templates returns all .yaml files in directory
    - Test 2: TemplateManager.get_template loads specific template by name
    - Test 3: TemplateManager.create_template saves new template to YAML
    - Test 4: TemplateManager validates template before saving
  </behavior>
  <action>
    Implement in scripts/mail_manager/templates.py:
    1. TemplateManager class with:
       - __init__(self, templates_dir: str)
       - list_templates(self) -> list[str]: Return template names without .yaml extension
       - get_template(self, name: str) -> Template: Load template by name
       - create_template(self, name: str, content: str, required_vars: list[str] | None = None) -> Template
       - validate_variables(self, template: Template, variables: dict) -> list[str]: Return missing required vars

    2. Create templates directory if not exists in create_template

    3. Follow existing patterns from rules.py for YAML handling
  </action>
  <verify>
    <automated>python -m pytest tests/test_templates.py::test_template_manager -v</automated>
  </verify>
  <done>TemplateManager class fully functional, all tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Add validate_variables for pre-send validation</name>
  <files>scripts/mail_manager/templates.py, tests/test_templates.py</files>
  <action>
    Implement in scripts/mail_manager/templates.py:
    1. validate_variables(template: Template, variables: dict) -> tuple[bool, list[str]]
       - Check if all required_vars are present in variables dict
       - Return (True, []) if all present
       - Return (False, missing_vars) if any missing

    2. Add substitute_variables(template: Template, variables: dict) -> str
       - Replace {{var}} with values from variables dict
       - Raise ValueError if any required variable missing

    3. Add tests for both functions
  </action>
  <verify>
    <automated>python -m pytest tests/test_templates.py -v --tb=short</automated>
  </verify>
  <done>All template tests pass with 80%+ coverage</done>
</task>

</tasks>

<verification>
- pytest tests/test_templates.py passes
- Coverage for templates.py >= 80%
- mypy templates.py passes with no errors
- ruff check passes
</verification>

<success_criteria>
- Template module loads YAML templates from account directory
- Variable extraction finds {{var}} patterns correctly
- Validation detects missing required variables
- TemplateManager provides list/get/create operations
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience-enhancement/05-01-SUMMARY.md`
</output>
