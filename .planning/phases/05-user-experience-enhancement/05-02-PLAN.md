---
phase: 05-user-experience-enhancement
plan: 02
type: execute
wave: 1
depends_on: ["05-00"]
files_modified: [scripts/mail_manager/detail.py, tests/test_detail.py]
autonomous: true
requirements: [DET-01, DET-02, DET-03, DET-04, DET-05]
user_setup: []
must_haves:
  truths:
    - "User can see email detail in formatted Markdown"
    - "User can see all headers (from, to, cc, date, subject)"
    - "User can see classification info (importance, category)"
    - "User can see attachments with preview links"
    - "User can see thread context (parent/reply emails)"
  artifacts:
    - path: "scripts/mail_manager/detail.py"
      provides: "Email detail formatting functions"
      exports: ["format_email_detail", "format_headers", "format_attachments_detail", "format_thread_context"]
    - path: "tests/test_detail.py"
      provides: "Test coverage for detail module"
      min_lines: 120
  key_links:
    - from: "format_email_detail"
      to: "email dict"
      via: "function call"
      pattern: "def format_email_detail"
    - from: "format_attachments_detail"
      to: "AttachmentServer"
      via: "preview URL generation"
      pattern: "127\\.0\\.0\\.1:\\d+"
---

<objective>
Implement enhanced email detail formatting with Markdown output, full headers, classification info, attachment links, and thread context.

Purpose: Provide rich, readable email detail view for CLI users with all relevant information in one place.
Output: Detail formatting module with comprehensive email display functions.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

## Existing Patterns

From `scripts/mail_cli.py:cmd_read` - current read output:
```python
table_md = _render_table(
    "email_table.md.j2",
    {"rows": [...]},
)
```

From `scripts/mail_cli.py:cmd_attachments` - attachment URL generation:
```python
rel_path = os.path.relpath(local_path, attachments_dir)
encoded_path = quote(rel_path)
url = f"http://127.0.0.1:{port}/{encoded_path}"
```

From `scripts/mail_manager/db.py` - email dict structure:
```python
email_dict = {
    "message_id": str,
    "subject": str,
    "sender": str,
    "recipient": str,
    "cc": str,
    "date": str,
    "body_text": str,
    "importance": str,  # from Phase 4
    "category": str,    # from Phase 4
    "classification_confidence": float,
    "attachments": list[dict],
}
```

## Output Format Example

```markdown
## Email: Project Update

**From:** John Doe <john@example.com>
**To:** me@company.com
**Cc:** team@company.com
**Date:** 2026-04-04 10:30:00

### Classification
- **Importance:** high (confidence: 0.85)
- **Category:** work

### Attachments
1. [report.pdf](http://127.0.0.1:8080/xxx/report.pdf) - 2.3 MB
2. [data.xlsx](http://127.0.0.1:8080/xxx/data.xlsx) - 156 KB

### Thread Context
- [Parent] Re: Previous discussion (2026-04-03)
- **[Current] Project Update** (2026-04-04)
- [Reply] Re: Project Update (2026-04-05)

---

Body text here...
```
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement format_email_detail with Markdown output</name>
  <files>scripts/mail_manager/detail.py, tests/test_detail.py</files>
  <behavior>
    - Test 1: format_email_detail returns Markdown string with headers
    - Test 2: Output includes subject as H2 heading
    - Test 3: Output includes body text with proper formatting
    - Test 4: Handles missing optional fields gracefully
  </behavior>
  <action>
    Implement in scripts/mail_manager/detail.py:
    1. format_email_detail(email: dict, port: int | None = None) -> str
       - Format email as Markdown with sections:
         - Header (## Subject)
         - Metadata table (From, To, Cc, Date)
         - Classification section (if available)
         - Attachments section (if any, with preview links)
         - Thread context section (if available)
         - Body text
       - Use markdown formatting (bold, lists, links)

    2. Helper function: _format_header_line(label: str, value: str | None) -> str
       - Return "**Label:** value" or skip if value is None/empty

    3. Follow immutability pattern - create new strings, don't mutate
  </action>
  <verify>
    <automated>python -m pytest tests/test_detail.py::test_format_email_detail -v</automated>
  </verify>
  <done>format_email_detail produces valid Markdown output</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement format_headers for full header display</name>
  <files>scripts/mail_manager/detail.py, tests/test_detail.py</files>
  <behavior>
    - Test 1: format_headers includes from, to, cc, date, subject
    - Test 2: Empty cc field is handled gracefully
    - Test 3: Date format is preserved as-is from database
  </behavior>
  <action>
    Implement in scripts/mail_manager/detail.py:
    1. format_headers(email: dict) -> str
       - Return Markdown-formatted header section
       - Include: From, To, Cc (if present), Date
       - Format as:
         ```
         **From:** sender
         **To:** recipient
         **Cc:** cc (if present)
         **Date:** date
         ```
       - Skip lines for empty/None values
  </action>
  <verify>
    <automated>python -m pytest tests/test_detail.py::test_format_headers -v</automated>
  </verify>
  <done>format_headers produces correct header block</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement format_classification for classification display</name>
  <files>scripts/mail_manager/detail.py, tests/test_detail.py</files>
  <behavior>
    - Test 1: format_classification shows importance and category
    - Test 2: format_classification shows confidence score
    - Test 3: Returns empty string if no classification data
    - Test 4: Handles uncategorized emails gracefully
  </behavior>
  <action>
    Implement in scripts/mail_manager/detail.py:
    1. format_classification(email: dict) -> str
       - Return Markdown section for classification:
         ```
         ### Classification
         - **Importance:** high (confidence: 0.85)
         - **Category:** work
         ```
       - Return empty string if importance is "normal" and category is "uncategorized"
       - Show confidence only if > 0.5 and not manual_override
  </action>
  <verify>
    <automated>python -m pytest tests/test_detail.py::test_format_classification -v</automated>
  </verify>
  <done>format_classification shows classification info correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Implement format_attachments_detail with preview links</name>
  <files>scripts/mail_manager/detail.py, tests/test_detail.py</files>
  <behavior>
    - Test 1: format_attachments_detail generates preview URLs
    - Test 2: Shows filename, size, and content type
    - Test 3: Returns empty string if no attachments
    - Test 4: Port is included in URL generation
  </behavior>
  <action>
    Implement in scripts/mail_manager/detail.py:
    1. format_attachments_detail(email: dict, port: int, attachments_dir: str) -> str
       - Return Markdown list of attachments with links:
         ```
         ### Attachments
         1. [filename.pdf](http://127.0.0.1:8080/path/filename.pdf) - 2.3 MB
         2. [image.png](http://127.0.0.1:8080/path/image.png) - 156 KB, image/png
         ```
       - Use human-readable file sizes (KB, MB, GB)
       - Generate relative path from attachments_dir to local_path

    2. Helper: _format_file_size(size_bytes: int) -> str
       - Return human-readable size (e.g., "2.3 MB", "156 KB")
  </action>
  <verify>
    <automated>python -m pytest tests/test_detail.py::test_format_attachments_detail -v</automated>
  </verify>
  <done>format_attachments_detail shows attachments with preview URLs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Implement format_thread_context for thread display</name>
  <files>scripts/mail_manager/detail.py, tests/test_detail.py</files>
  <behavior>
    - Test 1: format_thread_context shows parent emails
    - Test 2: format_thread_context shows reply emails
    - Test 3: Current email is highlighted in the thread
    - Test 4: Returns empty string if no thread context
  </behavior>
  <action>
    Implement in scripts/mail_manager/detail.py:
    1. format_thread_context(email: dict, timeline: list[dict], current_message_id: str) -> str
       - Return Markdown section showing thread:
         ```
         ### Thread Context
         - [Parent] Previous subject (2026-04-03)
         - **[Current] This subject** (2026-04-04)
         - [Reply] Re: This subject (2026-04-05)
         ```
       - Mark current email with bold
       - Show truncated subject (max 50 chars)
       - Sort by date ascending

    2. Handle empty timeline gracefully
  </action>
  <verify>
    <automated>python -m pytest tests/test_detail.py::test_format_thread_context -v</automated>
  </verify>
  <done>format_thread_context shows thread timeline correctly</done>
</task>

</tasks>

<verification>
- pytest tests/test_detail.py passes
- Coverage for detail.py >= 80%
- mypy detail.py passes
- ruff check passes
</verification>

<success_criteria>
- Email detail output is formatted as Markdown
- All headers visible (from, to, cc, date, subject)
- Classification info displayed (importance, category, confidence)
- Attachments listed with preview links
- Thread context shows parent/reply emails
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience-enhancement/05-02-SUMMARY.md`
</output>
