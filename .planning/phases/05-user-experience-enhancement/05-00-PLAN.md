---
phase: 05-user-experience-enhancement
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test files exist for templates module"
    - "Test files exist for email detail formatting"
    - "Test files exist for batch mark operations"
  artifacts:
    - path: "tests/test_templates.py"
      provides: "Test stubs for template module"
      min_lines: 20
    - path: "tests/test_detail.py"
      provides: "Test stubs for email detail formatting"
      min_lines: 20
    - path: "tests/test_batch_mark.py"
      provides: "Test stubs for batch mark operations"
      min_lines: 20
  key_links: []
---

<objective>
Create test stubs and empty module skeletons for Phase 5: User Experience Enhancement.

Purpose: Establish TDD foundation with failing tests that define expected behavior for templates, email detail formatting, and batch mark operations.
Output: Test files with placeholder test cases, empty module files.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

## Existing Patterns

From `tests/test_classifier.py` - test file structure:
```python
import pytest
from mail_manager.classifier import Classification, EmailClassifier
```

From `scripts/mail_manager/models.py` - dataclass patterns:
```python
@dataclass
class Classification:
    importance: str
    category: str
    confidence: float
    matched_rules: list[str] = field(default_factory=list)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for templates module</name>
  <files>tests/test_templates.py, scripts/mail_manager/templates.py</files>
  <action>
    Create tests/test_templates.py with placeholder test cases:
    - test_load_template_yaml
    - test_template_with_variables
    - test_template_missing_required_variable
    - test_list_templates
    - test_create_template

    Create empty scripts/mail_manager/templates.py with:
    - Template dataclass (name, content, required_vars)
    - TemplateManager class stub (load, list, create, validate)

    Tests should fail with NotImplementedError or ImportError initially.
  </action>
  <verify>
    <automated>python -c "import tests.test_templates; print('OK')" 2>&1 | head -5</automated>
  </verify>
  <done>Test file exists with 5+ placeholder tests, templates.py exists with class stubs</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for email detail formatting</name>
  <files>tests/test_detail.py, scripts/mail_manager/detail.py</files>
  <action>
    Create tests/test_detail.py with placeholder test cases:
    - test_format_email_detail_markdown
    - test_format_full_headers
    - test_format_classification_info
    - test_format_attachments_with_links
    - test_format_thread_context

    Create empty scripts/mail_manager/detail.py with:
    - format_email_detail function stub
    - format_headers function stub
    - format_thread_context function stub

    Tests should fail with NotImplementedError initially.
  </action>
  <verify>
    <automated>python -c "import tests.test_detail; print('OK')" 2>&1 | head -5</automated>
  </verify>
  <done>Test file exists with 5+ placeholder tests, detail.py exists with function stubs</done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs for batch mark operations</name>
  <files>tests/test_batch_mark.py</files>
  <action>
    Create tests/test_batch_mark.py with placeholder test cases:
    - test_batch_mark_read_multiple
    - test_batch_mark_starred_multiple
    - test_batch_mark_by_search_results
    - test_batch_add_tags
    - test_batch_remove_tags

    These tests will modify db.py and mail_cli.py in subsequent plans.

    Tests should fail with AttributeError for missing batch methods initially.
  </action>
  <verify>
    <automated>python -c "import tests.test_batch_mark; print('OK')" 2>&1 | head -5</automated>
  </verify>
  <done>Test file exists with 5+ placeholder tests</done>
</task>

</tasks>

<verification>
- All test files importable without syntax errors
- Each test file has at least 5 test functions
- Running pytest on these files shows failures (expected for TDD)
</verification>

<success_criteria>
- 3 test files created with placeholder tests
- 2 empty module files created (templates.py, detail.py)
- All imports work without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-user-experience-enhancement/05-00-SUMMARY.md`
</output>
