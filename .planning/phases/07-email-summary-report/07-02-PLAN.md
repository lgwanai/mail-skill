---
phase: 07-email-summary-report
plan: 02
type: tdd
wave: 2
depends_on: ["07-00", "07-01"]
files_modified: [scripts/mail_manager/summary_report.py, scripts/mail_manager/llm/prompts.py]
autonomous: true
requirements: [SUMMARY-03]
user_setup: []
must_haves:
  truths:
    - All individual summaries can be aggregated into an overall summary
    - Overall summary contains overview, key themes, action items, deadlines
    - Priority recommendations help user focus on most important items
  artifacts:
    - path: scripts/mail_manager/summary_report.py
      provides: Overall summary generation
      exports: ["OverallSummary", "generate_overall_summary"]
      contains: "class OverallSummary"
  key_links:
    - from: scripts/mail_manager/summary_report.py
      to: scripts/mail_manager/llm/client.py
      via: "LLMClient.chat() for aggregation"
      pattern: "llm_client\\.chat"
---

<objective>
Implement overall summary generation that aggregates individual email summaries.

Purpose: Enable SUMMARY-03 (overall summary with consolidated action items).
Output: OverallSummary dataclass and generate_overall_summary function.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-email-summary-report/07-RESEARCH.md
@.planning/phases/07-email-summary-report/07-00-SUMMARY.md
@.planning/phases/07-email-summary-report/07-01-SUMMARY.md

<interfaces>
<!-- Key types from prior plan. -->

From scripts/mail_manager/summary_report.py (Plan 01):
```python
@dataclass
class EmailSummary:
    subject: str
    key_points: list[str]
    action_items: list[str]
    deadline: str | None
    priority: str
    one_liner: str
```
</interfaces>
</context>

<feature>
  <name>Overall Summary Generation</name>
  <files>scripts/mail_manager/summary_report.py, tests/test_summary_report.py</files>
  <behavior>
    - OverallSummary: dataclass with overview, key_themes, all_action_items, upcoming_deadlines, recommended_priority
    - generate_overall_summary: dict[sender, list[EmailSummary]] + LLMClient -> OverallSummary
    - Consolidates all action items with sender attribution
    - Extracts deadlines and sorts by date
    Cases: Empty summaries returns empty OverallSummary; single email still produces valid summary
  </behavior>
  <implementation>
1. Create OverallSummary dataclass
2. Add OVERALL_SUMMARY_PROMPT to prompts.py
3. Implement generate_overall_summary that:
   - Formats all EmailSummary objects as text
   - Calls LLM with structured prompt
   - Parses JSON response
   - Falls back gracefully on parse errors
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OVERALL_SUMMARY_PROMPT to prompts.py</name>
  <files>scripts/mail_manager/llm/prompts.py, tests/test_summary_report.py</files>
  <behavior>
    - Test: OVERALL_SUMMARY_PROMPT exists and requests JSON with overview, key_themes, all_action_items, upcoming_deadlines, recommended_priority
  </behavior>
  <action>Add OVERALL_SUMMARY_PROMPT to scripts/mail_manager/llm/prompts.py:

The prompt should request JSON output with:
- overview: 2-3 sentence overview
- key_themes: list of main themes
- all_action_items: list of {item, sender, priority}
- upcoming_deadlines: list of {date, description, sender}
- recommended_priority: list of top 3 items to address

Use the exact format from 07-RESEARCH.md Pattern 3.</action>
  <verify>
    <automated>grep -n "OVERALL_SUMMARY_PROMPT" scripts/mail_manager/llm/prompts.py</automated>
  </verify>
  <done>OVERALL_SUMMARY_PROMPT exists with structured JSON format request</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement OverallSummary dataclass and generate_overall_summary</name>
  <files>scripts/mail_manager/summary_report.py, tests/test_summary_report.py</files>
  <behavior>
    - Test 1: generate_overall_summary returns OverallSummary with all fields
    - Test 2: generate_overall_summary handles empty summaries
    - Test 3: generate_overall_summary handles JSON parse errors with fallback
    - Test 4: action items include sender attribution
  </behavior>
  <action>RED: Write tests in TestOverallSummary class for overall summary behavior.
Mock LLMClient to return structured JSON response.

GREEN: Add to scripts/mail_manager/summary_report.py:
1. OverallSummary dataclass with fields: overview, key_themes, all_action_items, upcoming_deadlines, recommended_priority
2. generate_overall_summary(llm_client, sender_summaries) -> OverallSummary
   - Format sender summaries as text
   - Call llm_client.chat() with temperature=0.3
   - Parse JSON from response (handle markdown code blocks)
   - Return OverallSummary with fallback on parse errors

Reference 07-RESEARCH.md Pattern 3 for implementation.</action>
  <verify>
    <automated>pytest tests/test_summary_report.py::TestOverallSummary -x -v</automated>
  </verify>
  <done>generate_overall_summary passes all tests, produces structured overall summary</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_summary_report.py::TestOverallSummary -x -v`
</verification>

<success_criteria>
- OverallSummary dataclass holds aggregated summary data
- generate_overall_summary consolidates all individual summaries
- Action items are attributed to senders
- Deadlines are extracted and sorted
- JSON parsing failures are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-summary-report/07-02-SUMMARY.md`
</output>
