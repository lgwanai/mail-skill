---
phase: 07-email-summary-report
plan: 01
type: tdd
wave: 1
depends_on: ["07-00"]
files_modified: [scripts/mail_manager/summary_report.py]
autonomous: true
requirements: [SUMMARY-01, SUMMARY-02]
user_setup: []
must_haves:
  truths:
    - Emails can be grouped by sender
    - Each email can be summarized with key points and action items
    - EmailSummary dataclass holds structured summary data
  artifacts:
    - path: scripts/mail_manager/summary_report.py
      provides: Email grouping and individual summarization
      exports: ["group_emails_by_sender", "EmailSummary", "summarize_email"]
      min_lines: 100
  key_links:
    - from: scripts/mail_manager/summary_report.py
      to: scripts/mail_manager/db.py
      via: "search_emails() for filtering"
      pattern: "db\\.search_emails"
    - from: scripts/mail_manager/summary_report.py
      to: scripts/mail_manager/llm/client.py
      via: "LLMClient.chat() for summarization"
      pattern: "llm_client\\.chat"
---

<objective>
Implement email grouping by sender and individual email summarization.

Purpose: Enable SUMMARY-01 (group by sender) and SUMMARY-02 (individual summaries).
Output: summary_report.py with group_emails_by_sender, EmailSummary, and summarize_email.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-email-summary-report/07-RESEARCH.md
@.planning/phases/07-email-summary-report/07-VALIDATION.md

<interfaces>
<!-- Key types and contracts from existing code. -->

From scripts/mail_manager/llm/client.py:
```python
@dataclass
class LLMResponse:
    content: str
    model: str
    usage: dict[str, int]
    finish_reason: str

class LLMClient:
    def chat(
        self,
        messages: list[dict[str, str]],
        temperature: float = 0.7,
        max_tokens: int = 2000,
    ) -> LLMResponse: ...
```

From scripts/mail_manager/llm/prompts.py:
```python
THREAD_SUMMARY_PROMPT = "..."
REPLY_SYSTEM_PROMPT = "..."
```
</interfaces>
</context>

<feature>
  <name>Email Grouping and Individual Summarization</name>
  <files>scripts/mail_manager/summary_report.py, tests/test_summary_report.py</files>
  <behavior>
    - group_emails_by_sender: List of emails -> dict[sender, list[email]]
    - EmailSummary: dataclass with subject, key_points, action_items, deadline, priority, one_liner
    - summarize_email: Email dict + LLMClient -> EmailSummary
    Cases: Empty list returns empty dict; emails without sender are skipped
  </behavior>
  <implementation>
1. Create EmailSummary dataclass with fields: subject, key_points, action_items, deadline, priority, one_liner
2. Implement group_emails_by_sender using itertools.groupby
3. Implement summarize_email with structured LLM prompt requesting JSON output
4. Add EMAIL_SUMMARY_PROMPT to prompts.py (extend existing file)
5. Handle JSON parsing failures with fallback values
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EmailSummary dataclass and group_emails_by_sender</name>
  <files>scripts/mail_manager/summary_report.py, tests/test_summary_report.py</files>
  <behavior>
    - Test 1: group_emails_by_sender groups emails by sender field
    - Test 2: group_emails_by_sender returns empty dict for empty list
    - Test 3: group_emails_by_sender skips emails without sender
    - Test 4: EmailSummary dataclass has all required fields
  </behavior>
  <action>RED: Write tests in TestGroupEmailsBySender class for grouping behavior.

GREEN: Create scripts/mail_manager/summary_report.py with:
1. EmailSummary dataclass (subject, key_points, action_items, deadline, priority, one_liner)
2. group_emails_by_sender(emails: list[dict]) -> dict[str, list[dict]]
   - Sort by sender, use itertools.groupby
   - Skip emails with empty/None sender
   - Sort each group by date

Follow the patterns from 07-RESEARCH.md Pattern 1.</action>
  <verify>
    <automated>pytest tests/test_summary_report.py::TestGroupEmailsBySender -x -v</automated>
  </verify>
  <done>group_emails_by_sender passes all tests, EmailSummary dataclass defined</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add EMAIL_SUMMARY_PROMPT to prompts.py</name>
  <files>scripts/mail_manager/llm/prompts.py, tests/test_summary_report.py</files>
  <behavior>
    - Test: EMAIL_SUMMARY_PROMPT exists and contains placeholders for sender, subject, date, body
  </behavior>
  <action>Add EMAIL_SUMMARY_PROMPT to scripts/mail_manager/llm/prompts.py:

The prompt should request JSON output with:
- key_points: list of main points
- action_items: list of required actions
- deadline: YYYY-MM-DD or null
- priority: high/medium/low
- one_liner: single sentence summary

Use the exact format from 07-RESEARCH.md Pattern 2.</action>
  <verify>
    <automated>grep -n "EMAIL_SUMMARY_PROMPT" scripts/mail_manager/llm/prompts.py</automated>
  </verify>
  <done>EMAIL_SUMMARY_PROMPT exists with JSON format request</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement summarize_email function</name>
  <files>scripts/mail_manager/summary_report.py, tests/test_summary_report.py</files>
  <behavior>
    - Test 1: summarize_email returns EmailSummary with parsed JSON
    - Test 2: summarize_email handles malformed JSON with fallback
    - Test 3: summarize_email truncates long body_text (>2000 chars)
  </behavior>
  <action>RED: Write tests in TestSummarizeEmail class for summarization behavior.
Mock LLMClient to return structured JSON response.

GREEN: Implement summarize_email(llm_client, email) -> EmailSummary:
1. Truncate body_text to 2000 chars if needed
2. Format prompt with email details
3. Call llm_client.chat() with temperature=0.3
4. Parse JSON from response (handle markdown code blocks)
5. Return EmailSummary with fallback on parse errors

Reference 07-RESEARCH.md Pattern 2 for implementation.</action>
  <verify>
    <automated>pytest tests/test_summary_report.py::TestSummarizeEmail -x -v</automated>
  </verify>
  <done>summarize_email passes all tests, handles JSON parsing errors gracefully</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_summary_report.py::TestGroupEmailsBySender tests/test_summary_report.py::TestSummarizeEmail -x -v`
</verification>

<success_criteria>
- group_emails_by_sender correctly groups emails by sender
- EmailSummary dataclass holds all summary fields
- summarize_email produces structured summaries via LLM
- JSON parsing failures are handled gracefully
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-summary-report/07-01-SUMMARY.md`
</output>
