---
phase: 07-email-summary-report
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_summary_report.py, tests/conftest.py]
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - Test file exists with stubs for all SUMMARY requirements
    - Fixtures provide sample email groups and mock LLM responses
  artifacts:
    - path: tests/test_summary_report.py
      provides: Test stubs for SUMMARY-01~04
      min_lines: 50
    - path: tests/conftest.py
      provides: Fixtures for summary report tests
      contains: sample_email_group
---

<objective>
Create test stubs and fixtures for Phase 7: Email Summary Report.

Purpose: Establish TDD foundation with failing tests that define expected behavior.
Output: Test file with stubs and conftest fixtures.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-email-summary-report/07-RESEARCH.md
@.planning/phases/07-email-summary-report/07-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From scripts/mail_manager/llm/client.py:
```python
@dataclass
class LLMResponse:
    content: str
    model: str
    usage: dict[str, int]
    finish_reason: str

class LLMClient:
    def chat(
        self,
        messages: list[dict[str, str]],
        temperature: float = 0.7,
        max_tokens: int = 2000,
    ) -> LLMResponse: ...
```

From scripts/mail_manager/db.py:
```python
class MailDatabase:
    def search_emails(
        self,
        query: str | None = None,
        account: str | None = None,
        sender: str | None = None,
        date_from: str | None = None,
        date_to: str | None = None,
        limit: int = 100,
        offset: int = 0,
    ) -> list[dict]: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for SUMMARY-01~04</name>
  <files>tests/test_summary_report.py</files>
  <behavior>
    - Test 1: group_emails_by_sender groups emails correctly
    - Test 2: summarize_email returns structured EmailSummary
    - Test 3: generate_overall_summary returns OverallSummary
    - Test 4: format_summary_report produces valid Markdown
  </behavior>
  <action>Create tests/test_summary_report.py with test class stubs:

1. TestGroupEmailsBySender - test grouping logic
2. TestSummarizeEmail - test individual email summarization
3. TestOverallSummary - test overall summary generation
4. TestFormatSummaryReport - test Markdown formatting

Each test class should have placeholder test methods that will initially fail (import the modules that don't exist yet). Use pytest markers and skip for now since modules don't exist.

Reference the test patterns from tests/test_llm_client.py and tests/test_reply_assistant.py for consistency.</action>
  <verify>
    <automated>pytest tests/test_summary_report.py --collect-only -q 2>&1 | grep -E "test_summary_report|collected"</automated>
  </verify>
  <done>test_summary_report.py exists with 4 test classes and placeholder test methods</done>
</task>

<task type="auto">
  <name>Task 2: Add fixtures for summary report tests</name>
  <files>tests/conftest.py</files>
  <action>Extend tests/conftest.py with fixtures:

1. `sample_email_group` - dict mapping sender to list of sample emails
2. `mock_llm_summary_response` - mock LLM client returning structured JSON summary
3. `mock_llm_overall_response` - mock LLM client returning overall summary JSON

Use the existing patterns from conftest.py (mock_llm_client fixture style). Ensure fixtures are compatible with the test stubs created in Task 1.</action>
  <verify>
    <automated>grep -n "sample_email_group\|mock_llm_summary" tests/conftest.py | head -10</automated>
  </verify>
  <done>conftest.py has 3 new fixtures for summary report testing</done>
</task>

</tasks>

<verification>
Run `pytest tests/test_summary_report.py --collect-only` to verify test collection works.
</verification>

<success_criteria>
- tests/test_summary_report.py exists with 4 test classes
- tests/conftest.py has fixtures for summary tests
- Test collection succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-summary-report/07-00-SUMMARY.md`
</output>
