---
phase: 02-attachment-preview-service
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/mail_manager/server.py, tests/test_server.py]
autonomous: true
requirements: [ATCH-01, ATCH-02, ATCH-04, ATCH-05]

must_haves:
  truths:
    - "User can start the attachment server on an available port (8080-8099)"
    - "Server binds to 127.0.0.1 only, refusing external connections"
    - "Path traversal attacks are blocked (e.g., ../../../etc/passwd)"
    - "Server state persists to JSON file across CLI invocations"
    - "Symlink escapes are blocked"
  artifacts:
    - path: "scripts/mail_manager/server.py"
      provides: "AttachmentServer class with security constraints"
      min_lines: 80
      exports: ["AttachmentServer", "AttachmentHandler", "ServerState"]
    - path: "tests/test_server.py"
      provides: "Unit tests for server module"
      min_lines: 150
      exports: ["TestAttachmentServer", "TestPathValidation", "TestLocalhostBinding"]
  key_links:
    - from: "AttachmentHandler.translate_path"
      to: "attachments directory"
      via: "pathlib.resolve() + relative_to() validation"
      pattern: "relative_to.*attachments_dir"
    - from: "AttachmentServer.start"
      to: "ThreadingHTTPServer"
      via: "daemon thread with serve_forever"
      pattern: "daemon.*True"
---

<objective>
Implement the core AttachmentServer module with security constraints.

Purpose: Provide a secure local HTTP server for serving email attachments in the browser.
Output: server.py with AttachmentServer class, test_server.py with comprehensive unit tests
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-attachment-preview-service/02-RESEARCH.md
@.planning/phases/02-attachment-preview-service/02-VALIDATION.md

<interfaces>
<!-- Existing types and patterns from codebase -->

From scripts/mail_manager/errors.py:
```python
class ErrorCodes:
    USER_EMAIL_NOT_FOUND = "USER_EMAIL_NOT_FOUND"
    SERVER_DATABASE_ERROR = "SERVER_DATABASE_ERROR"

def error_response(code: str, message: str) -> dict
def success_response(data: Optional[dict] = None, message: str = "Success") -> dict
```

From scripts/mail_manager/models.py:
```python
@dataclass
class Attachment:
    filename: str
    content_type: str
    size: int
    local_path: str | None = None
```

From scripts/mail_cli.py:
```python
def _get_account_paths(config: dict[str, Any], email_address: str | None) -> dict[str, str]:
    # Returns: root, db_path, attach_path, eml_path, json_path, signature_path
```

From tests/conftest.py:
```python
@pytest.fixture
def temp_db_dir():
    """Create a temporary directory for database files."""
    with tempfile.TemporaryDirectory() as tmpdir:
        yield tmpdir
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test_server.py with security test cases</name>
  <files>tests/test_server.py</files>
  <behavior>
    - Test 1: find_available_port returns port in 8080-8099 range
    - Test 2: find_available_port raises error when no port available
    - Test 3: translate_path blocks basic path traversal (../)
    - Test 4: translate_path blocks URL-encoded traversal (%2e%2e%2f)
    - Test 5: translate_path blocks double-encoded traversal (%252e%252e)
    - Test 6: translate_path blocks symlink escape attempts
    - Test 7: server binds to 127.0.0.1 only (not 0.0.0.0)
    - Test 8: ServerState save/load persists correctly
    - Test 9: ServerState.is_running detects stale PIDs
  </behavior>
  <action>
    Create tests/test_server.py with comprehensive test classes:

    1. TestAttachmentServer - tests for server lifecycle
       - test_find_available_port_returns_in_range
       - test_find_available_port_raises_when_none_free
       - test_start_creates_daemon_thread
       - test_start_returns_port

    2. TestPathValidation - security tests (ATCH-04)
       - test_basic_traversal_blocked
       - test_encoded_traversal_blocked
       - test_double_encoded_blocked
       - test_symlink_escape_blocked
       - test_valid_path_allowed

    3. TestLocalhostBinding - network security tests (ATCH-02)
       - test_bind_address_is_localhost
       - test_server_address_format

    4. TestServerState - persistence tests (ATCH-05)
       - test_save_creates_json_file
       - test_load_returns_state
       - test_load_returns_none_for_missing_file
       - test_is_running_detects_stale_pid

    5. TestAttachmentHandler - HTTP handler tests
       - test_do_GET_returns_file_content
       - test_do_GET_returns_403_for_traversal
       - test_do_GET_sets_content_type

    Use fixtures from conftest.py. Create new fixtures for temp_attachments_dir and server_handler.
    Follow the existing test patterns from test_cli.py and test_client.py.
  </action>
  <verify>
    <automated>pytest tests/test_server.py -v --tb=short</automated>
  </verify>
  <done>tests/test_server.py exists with all 9+ test cases, pytest runs without import errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement AttachmentServer class in server.py</name>
  <files>scripts/mail_manager/server.py</files>
  <action>
    Create scripts/mail_manager/server.py with the following classes:

    1. ServerState (dataclass) - for persistence (ATCH-05)
       - Fields: port: int, pid: int, started_at: str
       - Methods: load(path), save(path), is_running()
       - Use JSON serialization

    2. AttachmentHandler(SimpleHTTPRequestHandler) - with security (ATCH-04)
       - Override translate_path() to add path validation
       - Use pathlib.resolve() + relative_to() for security
       - Block: basic traversal, encoded traversal, double-encoded, symlinks
       - Call super().translate_path() first, then validate
       - Set proper Content-Type via mimetypes.guess_type()

    3. AttachmentServer - main server class (ATCH-01, ATCH-02)
       - Fields: attachments_dir, port_range=(8080, 8099), server, thread, port
       - Methods:
         - find_available_port() -> int: iterate range, socket bind test
         - start() -> int: create ThreadingHTTPServer on 127.0.0.1, daemon thread
         - stop(): shutdown server
         - get_state_file(account_dir) -> Path: return state file path
       - Use ThreadingHTTPServer with daemon_threads=True
       - Handler wrapper to pass attachments_dir to AttachmentHandler

    Key implementation details from research:
    - Bind to "127.0.0.1" explicitly (NOT 0.0.0.0)
    - Use daemon=True on thread for clean exit
    - Use socket.bind() in context manager for port detection
    - Path validation: resolve both paths, check relative_to() doesn't raise

    Add new error codes to errors.py if needed:
    - SERVER_PORT_UNAVAILABLE = "SERVER_PORT_UNAVAILABLE"
    - SERVER_ATTACHMENT_NOT_FOUND = "SERVER_ATTACHMENT_NOT_FOUND"

    Follow existing code style from client.py and db.py:
    - Use from __future__ import annotations
    - Use TYPE_CHECKING for external type hints
    - Add logging with logger = logging.getLogger(__name__)
  </action>
  <verify>
    <automated>pytest tests/test_server.py -v --cov=scripts/mail_manager/server --cov-fail-under=80</automated>
  </verify>
  <done>server.py exists with all 3 classes, all tests pass, coverage >= 80%</done>
</task>

</tasks>

<verification>
1. Run unit tests: pytest tests/test_server.py -v
2. Run coverage: pytest tests/test_server.py --cov=scripts/mail_manager/server --cov-fail-under=80
3. Type check: mypy scripts/mail_manager/server.py
4. Lint: ruff check scripts/mail_manager/server.py tests/test_server.py

Security verification:
- Path traversal tests cover: basic (../), encoded (%2e%2e), double-encoded
- Symlink escape test exists
- Localhost binding test verifies 127.0.0.1
</verification>

<success_criteria>
1. AttachmentServer can find available port in 8080-8099 range
2. Server binds to 127.0.0.1 only (security verified)
3. Path traversal attacks are blocked (../, encoded, double-encoded, symlinks)
4. ServerState persists to JSON and detects stale processes
5. All tests pass with >= 80% coverage
6. mypy passes with no errors
7. ruff passes with no warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-attachment-preview-service/02-01-SUMMARY.md`
</output>
