---
phase: 04-smart-classification
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths: []
  artifacts:
    - path: "scripts/mail_manager/classifier.py"
      provides: "Classification and Rule dataclasses"
      min_lines: 20
    - path: "scripts/mail_manager/rules.py"
      provides: "Rule loader skeleton"
      min_lines: 10
    - path: "config/classification_rules.yaml"
      provides: "Default classification rules"
      min_lines: 30
    - path: "tests/test_classifier.py"
      provides: "Test stubs for classifier"
      min_lines: 20
  key_links: []
---

<objective>
Create test stubs and empty modules for TDD foundation, establishing the Classification dataclass and default rule configuration.

Purpose: Enable TDD workflow for subsequent plans by providing test scaffolds and type definitions.
Output: Empty modules with dataclasses, test file with stubs, default rules YAML.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-smart-classification/04-RESEARCH.md
@.planning/phases/04-smart-classification/04-VALIDATION.md

<interfaces>
<!-- Key patterns from existing codebase -->

From scripts/mail_manager/models.py:
```python
@dataclass
class Attachment:
    filename: str
    content_type: str
    size: int
    local_path: str | None = None

@dataclass
class CommandResult:
    status: str
    code: str | None = None
    message: str | None = None
    data: dict | None = None
```

From scripts/mail_manager/db.py:
```python
def _init_db(self) -> None:
    # Migration pattern: add columns if they don't exist
    cursor.execute("PRAGMA table_info(emails)")
    columns = [col[1] for col in cursor.fetchall()]
    if "new_column" not in columns:
        cursor.execute("ALTER TABLE emails ADD COLUMN new_column TYPE")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Classification and Rule dataclasses</name>
  <files>scripts/mail_manager/classifier.py</files>
  <action>
Create classifier.py module with dataclasses (NO implementation logic):

1. Create Classification dataclass:
   - importance: str (critical/high/normal/low)
   - category: str (work/personal/notification/promo/uncategorized)
   - confidence: float (0.0-1.0)
   - matched_rules: list[str]
   - manual_override: bool = False

2. Create Rule dataclass:
   - name: str
   - rule_type: str (sender/keyword/sender_pattern)
   - patterns: list[str]
   - importance: str | None = None
   - category: str | None = None
   - weight: float = 1.0

3. Create ClassificationResult dataclass for batch operations:
   - email_id: str
   - classification: Classification
   - processing_time_ms: float

Do NOT implement classify() logic - just dataclass definitions.
Use from __future__ import annotations for Python 3.8 compatibility.
Follow the pattern from models.py dataclasses.
  </action>
  <verify>
    <automated>python -c "from scripts.mail_manager.classifier import Classification, Rule, ClassificationResult; print('OK')"</automated>
  </verify>
  <done>Classifier module exists with Classification, Rule, ClassificationResult dataclasses that can be imported</done>
</task>

<task type="auto">
  <name>Task 2: Create rule loader skeleton</name>
  <files>scripts/mail_manager/rules.py</files>
  <action>
Create rules.py module with skeleton functions:

1. Define DEFAULT_RULES_PATH constant: "config/classification_rules.yaml"

2. Create load_rules function signature (stub):
   - def load_rules(path: str | None = None) -> list[Rule]:
   - Returns empty list (stub implementation)
   - Include docstring explaining YAML structure

3. Create get_default_rules function (stub):
   - def get_default_rules() -> list[Rule]:
   - Returns list of Rule objects with hardcoded defaults

Do NOT implement YAML loading - just signatures and docstrings.
  </action>
  <verify>
    <automated>python -c "from scripts.mail_manager.rules import load_rules, get_default_rules; print('OK')"</automated>
  </verify>
  <done>Rules module exists with load_rules and get_default_rules function signatures</done>
</task>

<task type="auto">
  <name>Task 3: Create default classification rules YAML</name>
  <files>config/classification_rules.yaml</files>
  <action>
Create config/classification_rules.yaml with default rules based on RESEARCH.md patterns:

1. Create config/ directory if it doesn't exist

2. Include rules for:
   - Critical senders (management emails)
   - Urgent keywords (紧急, urgent, 重要, important, asap)
   - Verification codes (验证码, verification code, 安全码)
   - Promotional keywords (促销, 优惠, unsubscribe, 退订, newsletter)
   - Work domain patterns (@company.com style patterns)
   - Personal indicators (family/friend domains)

3. Each rule must have:
   - name: descriptive name
   - type: sender/keyword/sender_pattern
   - patterns: list of strings
   - importance or category assignment
   - weight: confidence weighting (1.0-2.0)

Follow the exact YAML structure from RESEARCH.md Pattern 2.
  </action>
  <verify>
    <automated>python -c "import yaml; rules = yaml.safe_load(open('config/classification_rules.yaml')); assert 'rules' in rules; print('OK')"</automated>
  </verify>
  <done>YAML file exists with valid structure containing rules list</done>
</task>

<task type="auto">
  <name>Task 4: Create test stubs for classifier</name>
  <files>tests/test_classifier.py</files>
  <action>
Create tests/test_classifier.py with test stubs (placeholder tests):

1. Create test functions that are marked to be implemented:
   - test_classification_dataclass(): Test Classification creation
   - test_rule_dataclass(): Test Rule creation
   - test_load_rules_default(): Test loading default rules
   - test_classify_sender_match(): Test sender-based classification
   - test_classify_keyword_match(): Test keyword-based classification
   - test_classify_aggregation(): Test rule aggregation
   - test_classify_confidence(): Test confidence calculation
   - test_manual_reclassify(): Test manual reclassification

2. Each test should:
   - Have proper docstring explaining what will be tested
   - Use pytest.raises or placeholder assertions
   - Mark as "skip" with reason "Implementation pending"

3. Include imports for Classification, Rule, load_rules from classifier module

Do NOT implement test logic - just stubs with proper structure.
  </action>
  <verify>
    <automated>pytest tests/test_classifier.py --collect-only -q 2>&1 | grep -c "test_" | xargs -I{} sh -c 'test {} -ge 7 && echo OK'</automated>
  </verify>
  <done>Test file exists with at least 7 test function stubs</done>
</task>

</tasks>

<verification>
- All modules importable without errors
- YAML file parses correctly
- Test stubs collectable by pytest
</verification>

<success_criteria>
- classifier.py has Classification, Rule, ClassificationResult dataclasses
- rules.py has load_rules, get_default_rules function signatures
- config/classification_rules.yaml exists with valid YAML structure
- tests/test_classifier.py has test stubs for all planned test cases
</success_criteria>

<output>
After completion, create `.planning/phases/04-smart-classification/04-00-SUMMARY.md`
</output>
