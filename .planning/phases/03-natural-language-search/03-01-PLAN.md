---
phase: 03-natural-language-search
plan: 01
type: execute
wave: 1
depends_on: [03-00]
files_modified: [scripts/mail_manager/date_parser.py, tests/test_date_parser.py]
autonomous: true
requirements: [SRCH-01]
must_haves:
  truths:
    - "User can search with '昨天' and get emails from yesterday"
    - "User can search with '上周' and get emails from last week"
    - "User can search with '最近3天' and get emails from the last 3 days"
    - "Date parsing completes in under 50ms"
  artifacts:
    - path: "scripts/mail_manager/date_parser.py"
      provides: "Chinese date expression parser"
      exports: ["parse_date_expression", "DateRange"]
      min_lines: 50
    - path: "tests/test_date_parser.py"
      provides: "Unit tests for date parsing"
      contains: "test_parse_"
  key_links:
    - from: "date_parser.py"
      to: "datetime module"
      via: "stdlib datetime, timedelta"
      pattern: "from datetime import"
---

<objective>
Create a Chinese date expression parser that converts natural language date phrases to date ranges.

Purpose: Enable users to search emails using natural language date expressions like "上周", "昨天", "最近3天" instead of manual date filters.
Output: A tested date_parser.py module with parse_date_expression function.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-natural-language-search/03-RESEARCH.md

<interfaces>
<!-- Existing patterns from codebase -->
From scripts/mail_manager/errors.py:
```python
class ErrorCodes:
    USER_INVALID_PARAMETER = "USER_INVALID_PARAMETER"

def error_response(code: str, message: str) -> dict
def success_response(data: Optional[dict] = None, message: str = "Success") -> dict
```

From tests/conftest.py:
- Use pytest fixtures for test isolation
- Mock at library boundaries, not internal functions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create DateRange dataclass and parse_date_expression function</name>
  <files>scripts/mail_manager/date_parser.py, tests/test_date_parser.py</files>
  <behavior>
    - Test 1: parse_date_expression("昨天") returns DateRange for yesterday
    - Test 2: parse_date_expression("上周") returns DateRange for last week (Mon-Sun)
    - Test 3: parse_date_expression("最近3天") returns DateRange for last 3 days
    - Test 4: parse_date_expression("上个月") returns DateRange for last month
    - Test 5: parse_date_expression("invalid") returns None or raises appropriate error
  </behavior>
  <action>
    Update scripts/mail_manager/date_parser.py with full implementation:

    1. DateRange dataclass with date_from and date_to fields (both datetime.date objects)
       - Already created in Wave 0, verify structure is correct

    2. parse_date_expression(query: str, reference_date: date | None = None) -> DateRange | None
       - Takes a Chinese date expression and returns a DateRange
       - reference_date parameter for deterministic testing (defaults to today)
       - Returns None if expression cannot be parsed

    3. Support these patterns (regex-based, no LLM):
       - "昨天" -> yesterday
       - "前天" -> day before yesterday
       - "今天" -> today
       - "上周" -> last week (Mon-Sun of previous week)
       - "本周" -> this week (Mon-Sun of current week)
       - "最近N天" or "过去N天" -> last N days
       - "上个月" -> last calendar month
       - "本月" -> current month
       - "N月N日" or "N月N号" -> specific date (current year assumed)

    4. Each pattern returns DateRange with:
       - date_from: inclusive start date
       - date_to: inclusive end date

    Follow project conventions:
       - Use from __future__ import annotations for Python 3.8 compatibility
       - Use dataclass for DateRange
       - Add type annotations on all function signatures
       - Use modern Python type syntax (X | None instead of Optional[X])

    Update tests in tests/test_date_parser.py:
       - Remove skip markers from placeholder tests
       - Test each date pattern with a fixed reference_date
       - Test edge cases (month boundaries, year boundaries)
       - Test invalid inputs returning None
  </action>
  <verify>
    <automated>pytest tests/test_date_parser.py -v</automated>
  </verify>
  <done>
    - parse_date_expression correctly handles all specified Chinese date patterns
    - All tests pass with 80%+ coverage on date_parser.py
    - Function executes in under 50ms for any input
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add date extraction from mixed query strings</name>
  <files>scripts/mail_manager/date_parser.py, tests/test_date_parser.py</files>
  <behavior>
    - Test 1: extract_date_from_query("上周王总发的邮件") returns (DateRange, "王总发的邮件")
    - Test 2: extract_date_from_query("昨天收到的预算邮件") returns (DateRange, "收到的预算邮件")
    - Test 3: extract_date_from_query("预算讨论") returns (None, "预算讨论") (no date found)
  </behavior>
  <action>
    Update scripts/mail_manager/date_parser.py:

    1. extract_date_from_query(query: str, reference_date: date | None = None) -> tuple[DateRange | None, str]
       - Extracts date expression from the beginning or end of query
       - Returns tuple of (DateRange | None, remaining_query)
       - remaining_query has the date expression removed and is stripped

    2. Handle these positions:
       - Beginning: "上周的邮件" -> DateRange(last_week), "的邮件" -> "邮件"
       - End: "邮件昨天" -> DateRange(yesterday), "邮件"
       - Middle: Not extracted (too ambiguous, let keyword search handle it)

    3. Clean up remaining query:
       - Remove "的" connector after date expression
       - Strip whitespace
       - Return empty string if nothing remains

    Update tests in tests/test_date_parser.py:
       - Remove skip markers from placeholder tests
       - Test extraction from beginning of query
       - Test extraction from end of query
       - Test queries without date expressions
       - Test cleanup of remaining query text
  </action>
  <verify>
    <automated>pytest tests/test_date_parser.py -v</automated>
  </verify>
  <done>
    - extract_date_from_query correctly extracts dates from mixed query strings
    - Remaining query is clean with date expression removed
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: pytest tests/test_date_parser.py -v --cov=scripts/mail_manager/date_parser --cov-report=term-missing
2. Verify coverage >= 80%
3. Run: python -c "from scripts.mail_manager.date_parser import parse_date_expression, extract_date_from_query; print('Import OK')"
</verification>

<success_criteria>
- DateRange dataclass defined with date_from, date_to fields
- parse_date_expression handles: 昨天, 前天, 今天, 上周, 本周, 最近N天, 上个月, 本月, N月N日
- extract_date_from_query extracts dates from mixed query strings
- All tests pass with 80%+ coverage
- No external dependencies added (uses only stdlib datetime, re)
</success_criteria>

<output>
After completion, create `.planning/phases/03-natural-language-search/03-01-SUMMARY.md`
</output>
