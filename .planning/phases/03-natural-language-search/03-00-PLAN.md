---
phase: 03-natural-language-search
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [scripts/mail_manager/date_parser.py, scripts/mail_manager/query_parser.py, tests/test_date_parser.py, tests/test_query_parser.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "Test files exist and are importable"
    - "Module files exist with minimal structure"
    - "pytest can discover and run the test files"
  artifacts:
    - path: "tests/test_date_parser.py"
      provides: "Test stub for date parser"
      contains: "test_"
    - path: "tests/test_query_parser.py"
      provides: "Test stub for query parser"
      contains: "test_"
    - path: "scripts/mail_manager/date_parser.py"
      provides: "Empty date parser module"
      contains: "DateRange"
    - path: "scripts/mail_manager/query_parser.py"
      provides: "Empty query parser module"
      contains: "ParsedQuery"
  key_links: []
---

<objective>
Create test stubs and empty module files to enable TDD workflow for subsequent plans.

Purpose: Ensure Wave 1 plans can run their automated verify commands (pytest) without missing file errors.
Output: Empty module files with type definitions, and test files with passing placeholder tests.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Project conventions from existing test files -->
From tests/conftest.py:
- Use pytest fixtures for test isolation
- Mock at library boundaries

From scripts/mail_manager/errors.py:
- Error handling patterns
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create date_parser module with DateRange dataclass</name>
  <files>scripts/mail_manager/date_parser.py</files>
  <action>
    Create scripts/mail_manager/date_parser.py with minimal structure:

    ```python
    """Chinese date expression parser for natural language search."""
    from __future__ import annotations

    from dataclasses import dataclass
    from datetime import date


    @dataclass
    class DateRange:
        """Represents a date range with inclusive start and end dates."""
        date_from: date
        date_to: date


    def parse_date_expression(query: str, reference_date: date | None = None) -> DateRange | None:
        """Parse Chinese date expression to DateRange. To be implemented."""
        raise NotImplementedError("parse_date_expression not yet implemented")


    def extract_date_from_query(query: str, reference_date: date | None = None) -> tuple[DateRange | None, str]:
        """Extract date expression from query string. To be implemented."""
        raise NotImplementedError("extract_date_from_query not yet implemented")
    ```

    This provides:
    - DateRange dataclass for downstream plans to import
    - Function signatures with type hints
    - NotImplementedError to signal incomplete implementation
  </action>
  <verify>
    <automated>python -c "from scripts.mail_manager.date_parser import DateRange, parse_date_expression, extract_date_from_query; print('Import OK')"</automated>
  </verify>
  <done>
    - Module file exists with DateRange dataclass
    - Function signatures defined with proper type hints
    - Module is importable without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create query_parser module with ParsedQuery dataclass</name>
  <files>scripts/mail_manager/query_parser.py</files>
  <action>
    Create scripts/mail_manager/query_parser.py with minimal structure:

    ```python
    """Natural language query parser for email search."""
    from __future__ import annotations

    from dataclasses import dataclass

    from .date_parser import DateRange


    @dataclass
    class ParsedQuery:
        """Represents a parsed natural language query."""
        original_query: str
        date_range: DateRange | None
        sender: str | None
        keywords: str


    def parse_natural_query(query: str, reference_date=None) -> ParsedQuery:
        """Parse natural language query to ParsedQuery. To be implemented."""
        raise NotImplementedError("parse_natural_query not yet implemented")


    def match_senders(query_sender: str, sender_list: list[str]) -> list[str]:
        """Match sender pattern against sender list. To be implemented."""
        raise NotImplementedError("match_senders not yet implemented")
    ```

    This provides:
    - ParsedQuery dataclass for downstream plans to import
    - Function signatures with type hints
    - Import of DateRange from date_parser (tests module connectivity)
  </action>
  <verify>
    <automated>python -c "from scripts.mail_manager.query_parser import ParsedQuery, parse_natural_query, match_senders; print('Import OK')"</automated>
  </verify>
  <done>
    - Module file exists with ParsedQuery dataclass
    - Function signatures defined with proper type hints
    - DateRange import works
    - Module is importable without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Create test stubs with passing placeholder tests</name>
  <files>tests/test_date_parser.py, tests/test_query_parser.py</files>
  <action>
    Create tests/test_date_parser.py:

    ```python
    """Tests for date_parser module."""
    import pytest
    from scripts.mail_manager.date_parser import DateRange, parse_date_expression, extract_date_from_query


    class TestDateRange:
        """Tests for DateRange dataclass."""

        def test_date_range_is_dataclass(self):
            """DateRange should be a dataclass with date_from and date_to."""
            from dataclasses import fields
            field_names = [f.name for f in fields(DateRange)]
            assert "date_from" in field_names
            assert "date_to" in field_names


    class TestParseDateExpression:
        """Tests for parse_date_expression function. To be expanded in Plan 03-01."""

        @pytest.mark.skip(reason="Implementation in Plan 03-01")
        def test_parse_yesterday(self):
            """Test parsing '昨天'."""
            pass

        @pytest.mark.skip(reason="Implementation in Plan 03-01")
        def test_parse_last_week(self):
            """Test parsing '上周'."""
            pass


    class TestExtractDateFromQuery:
        """Tests for extract_date_from_query function. To be expanded in Plan 03-01."""

        @pytest.mark.skip(reason="Implementation in Plan 03-01")
        def test_extract_from_beginning(self):
            """Test extracting date from beginning of query."""
            pass
    ```

    Create tests/test_query_parser.py:

    ```python
    """Tests for query_parser module."""
    import pytest
    from scripts.mail_manager.query_parser import ParsedQuery, parse_natural_query, match_senders


    class TestParsedQuery:
        """Tests for ParsedQuery dataclass."""

        def test_parsed_query_is_dataclass(self):
            """ParsedQuery should be a dataclass with expected fields."""
            from dataclasses import fields
            field_names = [f.name for f in fields(ParsedQuery)]
            assert "original_query" in field_names
            assert "date_range" in field_names
            assert "sender" in field_names
            assert "keywords" in field_names


    class TestParseNaturalQuery:
        """Tests for parse_natural_query function. To be expanded in Plan 03-02."""

        @pytest.mark.skip(reason="Implementation in Plan 03-02")
        def test_parse_combined_query(self):
            """Test parsing query with date, sender, and keywords."""
            pass


    class TestMatchSenders:
        """Tests for match_senders function. To be expanded in Plan 03-02."""

        @pytest.mark.skip(reason="Implementation in Plan 03-02")
        def test_match_by_name(self):
            """Test matching sender by name substring."""
            pass
    ```

    These test files:
    - Have passing tests for dataclass structure (verifies Wave 0 artifacts)
    - Have skipped placeholder tests for Wave 1 and Wave 2 implementation
    - Allow pytest to run without errors or "no tests collected" warnings
  </action>
  <verify>
    <automated>pytest tests/test_date_parser.py tests/test_query_parser.py -v</automated>
  </verify>
  <done>
    - Both test files exist and are importable
    - pytest runs successfully with passing tests
    - Placeholder tests are skipped, not failing
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: pytest tests/test_date_parser.py tests/test_query_parser.py -v
2. Verify all tests pass (some skipped, none failing)
3. Run: python -c "from scripts.mail_manager.date_parser import DateRange; from scripts.mail_manager.query_parser import ParsedQuery; print('All imports OK')"
</verification>

<success_criteria>
- date_parser.py exists with DateRange dataclass and function stubs
- query_parser.py exists with ParsedQuery dataclass and function stubs
- Both test files exist with passing placeholder tests
- pytest discovers and runs tests without errors
- All modules are importable
</success_criteria>

<output>
After completion, create `.planning/phases/03-natural-language-search/03-00-SUMMARY.md`
</output>
