---
phase: 03-natural-language-search
plan: 02
type: execute
wave: 2
depends_on: [03-00, 03-01]
files_modified: [scripts/mail_manager/query_parser.py, tests/test_query_parser.py]
autonomous: true
requirements: [SRCH-02, SRCH-03]
must_haves:
  truths:
    - "User can search '王总发的邮件' and system extracts '王总' as sender"
    - "User can search '上周预算讨论' and system extracts date + keywords"
    - "Query parsing completes in under 100ms"
    - "Parsed query shows extracted components to user"
  artifacts:
    - path: "scripts/mail_manager/query_parser.py"
      provides: "Natural language query parser"
      exports: ["ParsedQuery", "parse_natural_query"]
      min_lines: 80
    - path: "tests/test_query_parser.py"
      provides: "Unit tests for query parser"
      contains: "test_parse_natural"
  key_links:
    - from: "query_parser.py"
      to: "date_parser.py"
      via: "import and call extract_date_from_query"
      pattern: "from .date_parser import"
---

<objective>
Create a natural language query parser that extracts date ranges, sender patterns, and keywords from user queries.

Purpose: Transform user's natural language search intent into structured search parameters.
Output: A tested query_parser.py module with ParsedQuery dataclass and parse_natural_query function.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-natural-language-search/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01 (date parser) -->
From scripts/mail_manager/date_parser.py:
```python
@dataclass
class DateRange:
    date_from: date
    date_to: date

def parse_date_expression(query: str, reference_date: date | None = None) -> DateRange | None
def extract_date_from_query(query: str, reference_date: date | None = None) -> tuple[DateRange | None, str]
```

<!-- Existing database patterns -->
From scripts/mail_manager/db.py:
```python
class MailDatabase:
    def search_emails(
        self,
        query: str | None = None,
        sender: str | None = None,
        date_from: str | None = None,
        date_to: str | None = None,
        ...
    ) -> list[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ParsedQuery dataclass and sender extraction</name>
  <files>scripts/mail_manager/query_parser.py, tests/test_query_parser.py</files>
  <behavior>
    - Test 1: extract_sender_from_query("王总发的邮件") returns ("王总", "邮件")
    - Test 2: extract_sender_from_query("来自gmail.com的邮件") returns ("gmail.com", "邮件")
    - Test 3: extract_sender_from_query("预算讨论") returns (None, "预算讨论")
    - Test 4: ParsedQuery has date_range, sender, keywords, original_query fields
  </behavior>
  <action>
    Update scripts/mail_manager/query_parser.py with full implementation:

    1. ParsedQuery dataclass with fields:
       - original_query: str (the raw user input)
       - date_range: DateRange | None
       - sender: str | None (extracted sender pattern)
       - keywords: str (remaining text for keyword search)
       - Already created in Wave 0, verify structure is correct

    2. extract_sender_from_query(query: str) -> tuple[str | None, str]
       - Extracts sender patterns from query
       - Patterns to match:
         - "X发的" or "X发来的" -> sender X at beginning
         - "来自X的" or "来自X" -> sender X
         - "from X" -> sender X (English pattern)
       - Returns (sender, remaining_query)
       - remaining_query has sender expression removed

    3. clean_keywords(text: str) -> str
       - Remove common stop words: "的", "邮件", "所有", "全部"
       - Strip whitespace
       - Return cleaned text for keyword search

    Follow project conventions:
       - Use from __future__ import annotations
       - Use dataclass for ParsedQuery
       - Import DateRange from date_parser (relative import: from .date_parser import DateRange)

    Update tests in tests/test_query_parser.py:
       - Remove skip markers from placeholder tests
       - Test sender extraction patterns
       - Test edge cases
  </action>
  <verify>
    <automated>pytest tests/test_query_parser.py -v</automated>
  </verify>
  <done>
    - ParsedQuery dataclass defined with all fields
    - extract_sender_from_query handles Chinese and English patterns
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement parse_natural_query function</name>
  <files>scripts/mail_manager/query_parser.py, tests/test_query_parser.py</files>
  <behavior>
    - Test 1: parse_natural_query("上周王总发的预算邮件") returns ParsedQuery with date_range, sender="王总", keywords="预算"
    - Test 2: parse_natural_query("昨天收到的邮件") returns ParsedQuery with date_range, sender=None, keywords=""
    - Test 3: parse_natural_query("预算讨论") returns ParsedQuery with no date, no sender, keywords="预算讨论"
    - Test 4: Parsing completes in under 100ms for any query
  </behavior>
  <action>
    Update scripts/mail_manager/query_parser.py:

    1. parse_natural_query(query: str, reference_date: date | None = None) -> ParsedQuery
       - Main entry point for natural language query parsing
       - Pipeline:
         a. Extract date expression using extract_date_from_query
         b. Extract sender from remaining text
         c. Clean keywords from final remaining text
         d. Return ParsedQuery with all components

    2. Implementation notes:
       - Order matters: date first, then sender, then keywords
       - Preserve original_query in ParsedQuery
       - Handle edge cases:
         - Empty query -> ParsedQuery with all None/empty
         - Query with only date -> ParsedQuery with date only
         - Query with only sender -> ParsedQuery with sender only

    Update tests in tests/test_query_parser.py:
       - Remove skip markers from placeholder tests
       - Test combined queries (date + sender + keywords)
       - Test queries with only some components
       - Test edge cases
       - Test performance (should be fast, no external calls)
  </action>
  <verify>
    <automated>pytest tests/test_query_parser.py -v</automated>
  </verify>
  <done>
    - parse_natural_query correctly parses combined queries
    - All components extracted in correct order
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add sender fuzzy matching helper</name>
  <files>scripts/mail_manager/query_parser.py, tests/test_query_parser.py</files>
  <behavior>
    - Test 1: match_senders("王总", ["王某某 <wang@example.com>", "张三 <zhang@example.com>"]) returns ["王某某 <wang@example.com>"]
    - Test 2: match_senders("wang", ["王某某 <wang@example.com>"]) returns ["王某某 <wang@example.com>"] (email match)
    - Test 3: match_senders("unknown", [...]) returns [] (no match)
  </behavior>
  <action>
    Add to scripts/mail_manager/query_parser.py:

    1. Helper function parse_sender_field(sender: str) -> tuple[str, str]
       - Parses "Name <email@domain.com>" format
       - Returns (name, email) tuple
       - Handles edge cases: no angle brackets, only email, etc.

    2. match_senders(query_sender: str, sender_list: list[str]) -> list[str]
       - Takes a sender pattern from query and a list of sender strings from DB
       - Returns list of matching senders (from sender_list)
       - Matching logic:
         a. Parse each sender to extract name and email
         b. Check if query_sender is substring of name (case-insensitive)
         c. Check if query_sender is substring of email (case-insensitive)
         d. Return all senders that match either condition
       - Does NOT require external fuzzy matching library (keep it simple)
       - Does NOT query database directly - receives sender_list as parameter

    3. Add SenderMatcher class (optional, for future enhancement):
       - Can be extended later with pinyin matching, nickname mapping
       - For now, just wraps match_senders function

    Update tests in tests/test_query_parser.py:
       - Remove skip markers from placeholder tests
       - Test name matching
       - Test email matching
       - Test no matches
       - Test mixed sender list
  </action>
  <verify>
    <automated>pytest tests/test_query_parser.py -v</automated>
  </verify>
  <done>
    - match_senders correctly identifies matching senders
    - Works with both name and email substrings
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: pytest tests/test_query_parser.py -v --cov=scripts/mail_manager/query_parser --cov-report=term-missing
2. Verify coverage >= 80%
3. Run: python -c "from scripts.mail_manager.query_parser import parse_natural_query, ParsedQuery; print('Import OK')"
4. Run: python -c "from scripts.mail_manager.query_parser import match_senders; print(match_senders('test', ['test@example.com']))"
</verification>

<success_criteria>
- ParsedQuery dataclass with original_query, date_range, sender, keywords fields
- parse_natural_query extracts date, sender, keywords from combined queries
- match_senders performs fuzzy sender matching without external dependencies
- All tests pass with 80%+ coverage
- No external dependencies added (uses only stdlib re)
</success_criteria>

<output>
After completion, create `.planning/phases/03-natural-language-search/03-02-SUMMARY.md`
</output>
