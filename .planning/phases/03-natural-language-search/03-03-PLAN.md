---
phase: 03-natural-language-search
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified: [scripts/mail_cli.py, scripts/mail_manager/db.py, tests/test_cli.py]
autonomous: true
requirements: [SRCH-04, SRCH-05]
must_haves:
  truths:
    - "User can run 'smart-search 上周王总发的预算邮件' and get correct results"
    - "User can see extracted query components in the response"
    - "Original 'search' command behavior is unchanged"
    - "Smart search completes within 2 seconds"
  artifacts:
    - path: "scripts/mail_cli.py"
      provides: "CLI command for smart-search"
      contains: "def cmd_smart_search"
    - path: "scripts/mail_manager/db.py"
      provides: "Helper to get unique senders"
      contains: "def get_unique_senders"
    - path: "tests/test_cli.py"
      provides: "Integration tests for smart-search command"
      contains: "test_smart_search"
  key_links:
    - from: "mail_cli.py"
      to: "query_parser.py"
      via: "import parse_natural_query"
      pattern: "from mail_manager.query_parser import"
    - from: "mail_cli.py"
      to: "db.py"
      via: "search_hybrid, search_emails, get_unique_senders"
      pattern: "isolated_db.search_hybrid"
---

<objective>
Integrate natural language query parser into CLI with new smart-search command.

Purpose: Allow users to search emails using natural language queries while maintaining backward compatibility.
Output: Working smart-search CLI command that displays extracted query components and returns matching emails.
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-natural-language-search/03-RESEARCH.md

<interfaces>
<!-- From Plan 03-01 and 03-02 -->
From scripts/mail_manager/query_parser.py (to be created):
```python
@dataclass
class ParsedQuery:
    original_query: str
    date_range: DateRange | None
    sender: str | None
    keywords: str

def parse_natural_query(query: str, reference_date: date | None = None) -> ParsedQuery
def match_senders(query_sender: str, sender_list: list[str]) -> list[str]
```

From scripts/mail_manager/date_parser.py (to be created):
```python
@dataclass
class DateRange:
    date_from: date
    date_to: date
```

<!-- Existing CLI patterns -->
From scripts/mail_cli.py:
```python
def cmd_search(args: Any, config: dict[str, Any], db: MailDatabase) -> None:
    """Search emails using FTS, vector, or hybrid search."""

def success_response(data: Optional[dict] = None, message: str = "Success") -> dict
def error_response(code: str, message: str) -> dict
```

From scripts/mail_manager/db.py:
```python
class MailDatabase:
    def search_hybrid(self, query: str, limit: int = 10) -> list[dict]
    def search_emails(self, ..., date_from: str | None, date_to: str | None, sender: str | None, ...) -> list[dict]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add get_unique_senders method to MailDatabase</name>
  <files>scripts/mail_manager/db.py, tests/test_db.py</files>
  <behavior>
    - Test 1: get_unique_senders() returns list of unique sender strings
    - Test 2: get_unique_senders() returns empty list for empty database
    - Test 3: get_unique_senders() handles duplicate senders correctly
  </behavior>
  <action>
    Add to scripts/mail_manager/db.py in MailDatabase class:

    def get_unique_senders(self) -> list[str]:
        """Get list of unique sender strings from database.

        Returns:
            list[str]: List of unique sender values (e.g., "Name <email@example.com>")
        """
        with self._get_connection() as conn:
            cursor = conn.cursor()
            cursor.execute("SELECT DISTINCT sender FROM emails WHERE sender IS NOT NULL AND sender != ''")
            rows = cursor.fetchall()
            return [row["sender"] for row in rows]

    Add tests to tests/test_db.py:
       - Test with populated database
       - Test with empty database
       - Test that duplicates are removed
       - Test that None/empty senders are excluded
  </action>
  <verify>
    <automated>pytest tests/test_db.py -v -k get_unique_senders</automated>
  </verify>
  <done>
    - get_unique_senders returns unique sender strings
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create cmd_smart_search CLI command</name>
  <files>scripts/mail_cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: smart-search "预算讨论" returns emails matching keywords
    - Test 2: smart-search "上周邮件" returns emails from last week
    - Test 3: smart-search "王总发的" returns emails from senders matching "王总"
    - Test 4: Response includes parsed_query showing extracted components
    - Test 5: Response completes within 2 seconds
  </behavior>
  <action>
    Add to scripts/mail_cli.py:

    1. Import at top:
       from mail_manager.query_parser import parse_natural_query, match_senders, ParsedQuery

    2. New function cmd_smart_search(args: Any, config: dict[str, Any], db: MailDatabase) -> None:
       Implementation:
       a. Parse natural language query using parse_natural_query
       b. Get unique senders from database for fuzzy matching
       c. If sender extracted, match against sender list
       d. Build search parameters from parsed query:
          - date_from/date_to from DateRange (convert to ISO string)
          - sender filters from matched senders (use OR logic for multiple)
          - keywords for hybrid search
       e. Execute search:
          - If keywords: use search_hybrid for semantic matching
          - If only filters: use search_emails with structured params
       f. Format response with parsed_query and results:
          {
            "status": "success",
            "data": {
              "parsed_query": {
                "original": "上周王总发的预算邮件",
                "date_range": {"from": "2026-03-28", "to": "2026-04-03"},
                "sender": ["王某某 <wang@example.com>"],
                "keywords": "预算"
              },
              "count": 3,
              "results": [...]
            }
          }

    3. Add argparse subparser for smart-search:
       smart_search_p = subparsers.add_parser("smart-search", help="Search emails using natural language")
       smart_search_p.add_argument("query", help="Natural language search query")
       smart_search_p.add_argument("--account", help="Account to search")
       smart_search_p.add_argument("--limit", type=int, default=20, help="Max results")

    4. Add command routing in main():
       elif args.command == "smart-search":
           cmd_smart_search(args, config, db)

    Add tests to tests/test_cli.py:
       - Test with mocked database
       - Test parsing output contains parsed_query
       - Test that original search command is unchanged (backward compat)
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -v -k smart_search</automated>
  </verify>
  <done>
    - smart-search command works with natural language queries
    - Response shows extracted query components
    - Original search command unchanged
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add performance optimization for sender caching</name>
  <files>scripts/mail_cli.py, tests/test_cli.py</files>
  <behavior>
    - Test 1: Second smart-search call is faster than first (sender list cached)
    - Test 2: Total query time under 2 seconds
  </behavior>
  <action>
    Optimize cmd_smart_search in scripts/mail_cli.py:

    1. Add sender list caching (simple module-level cache):
       _sender_cache: dict[str, tuple[float, list[str]]] = {}
       CACHE_TTL = 300  # 5 minutes

       def get_cached_senders(db: MailDatabase, account: str) -> list[str]:
           cache_key = account
           now = time.time()
           if cache_key in _sender_cache:
               cached_time, senders = _sender_cache[cache_key]
               if now - cached_time < CACHE_TTL:
                   return senders
           senders = db.get_unique_senders()
           _sender_cache[cache_key] = (now, senders)
           return senders

    2. Use get_cached_senders in cmd_smart_search instead of direct db call

    3. Add import time at top of file

    Add performance test to tests/test_cli.py:
       - Test that cache reduces subsequent calls (mock db to count calls)
       - Note: Actual timing test is optional, can be manual verification
  </action>
  <verify>
    <automated>pytest tests/test_cli.py -v -k smart_search</automated>
  </verify>
  <done>
    - Sender list is cached with 5-minute TTL
    - Subsequent searches are faster
    - All tests pass
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run: pytest tests/test_cli.py tests/test_db.py -v -k "smart_search or get_unique_senders"
2. Run: python scripts/mail_cli.py smart-search "test" --help
3. Run: python scripts/mail_cli.py search --help (verify unchanged)
4. Verify coverage: pytest --cov=scripts/mail_manager --cov=scripts/mail_cli --cov-report=term-missing
</verification>

<success_criteria>
- smart-search command accepts natural language queries
- Query components (date, sender, keywords) are extracted and shown to user
- Search executes within 2 seconds
- Original search command behavior unchanged (backward compatibility)
- Sender list cached for performance
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-natural-language-search/03-03-SUMMARY.md`
</output>
