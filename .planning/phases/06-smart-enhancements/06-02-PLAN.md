---
phase: 06-smart-enhancements
plan: 02
type: execute
wave: 1
depends_on: [06-00]
files_modified: [scripts/mail_manager/attachment_parser/__init__.py, scripts/mail_manager/attachment_parser/base.py, scripts/mail_manager/attachment_parser/pdf_parser.py, scripts/mail_manager/attachment_parser/excel_parser.py, scripts/mail_manager/attachment_parser/pptx_parser.py, scripts/mail_manager/attachment_parser/text_parser.py, tests/test_attachment_parser.py]
autonomous: true
requirements: [ATTACH-AI-01]

must_haves:
  truths:
    - "PDF files are parsed and text extracted"
    - "Excel files are parsed with all sheets"
    - "PowerPoint files are parsed with all slides"
    - "Text and markdown files are read correctly"
  artifacts:
    - path: "scripts/mail_manager/attachment_parser/__init__.py"
      provides: "Parser factory and exports"
      exports: ["parse_attachment", "get_parser"]
    - path: "scripts/mail_manager/attachment_parser/base.py"
      provides: "DocumentParser protocol"
    - path: "scripts/mail_manager/attachment_parser/pdf_parser.py"
      provides: "PDF parsing via PyMuPDF"
    - path: "scripts/mail_manager/attachment_parser/excel_parser.py"
      provides: "Excel parsing via openpyxl"
    - path: "scripts/mail_manager/attachment_parser/pptx_parser.py"
      provides: "PowerPoint parsing via python-pptx"
    - path: "scripts/mail_manager/attachment_parser/text_parser.py"
      provides: "Text/markdown file parsing"
  key_links:
    - from: "attachment_parser/__init__.py"
      to: "pdf_parser, excel_parser, pptx_parser, text_parser"
      via: "Parser registry and factory"
      pattern: "get_parser\\(.*suffix.*\\)"
---

<objective>
Create document parser infrastructure for attachment content extraction.

Purpose: Parse various document types (PDF, Excel, PowerPoint, text) for enhanced search indexing
Output: Protocol-based parser system with implementations for each document type
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-smart-enhancements/06-RESEARCH.md

## Parser Protocol Pattern

From RESEARCH.md Pattern 2:
```python
class DocumentParser(Protocol):
    def can_parse(self, file_path: Path) -> bool: ...
    def extract_text(self, file_path: Path) -> str: ...
    def extract_metadata(self, file_path: Path) -> dict[str, Any]: ...
```

## Dependencies (Already Installed)

- PyMuPDF (fitz) 1.26.3 for PDF
- openpyxl 3.1.5 for Excel
- python-pptx 1.0.2 for PowerPoint
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create parser protocol and base infrastructure</name>
  <files>scripts/mail_manager/attachment_parser/__init__.py, scripts/mail_manager/attachment_parser/base.py, tests/test_attachment_parser.py</files>
  <behavior>
    - Test 1: DocumentParser protocol defines can_parse, extract_text, extract_metadata
    - Test 2: get_parser() returns correct parser by file extension
    - Test 3: parse_attachment() dispatches to correct parser
  </behavior>
  <action>
Create parser infrastructure:

1. Create scripts/mail_manager/attachment_parser/__init__.py:
   - Export parse_attachment function
   - Export get_parser function
   - Parser registry dict mapping suffix to parser class

2. Create scripts/mail_manager/attachment_parser/base.py:
   - DocumentParser Protocol with can_parse, extract_text, extract_metadata
   - ParseResult dataclass with text, metadata fields

3. Create parse_attachment(file_path: Path) -> ParseResult factory function:
   - Get parser by extension
   - Call parser methods
   - Return ParseResult

Run tests: pytest tests/test_attachment_parser.py -x -k "protocol or factory"
  </action>
  <verify>
    <automated>pytest tests/test_attachment_parser.py -x -k "protocol or factory"</automated>
  </verify>
  <done>Parser protocol defined, factory function works, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement PDF, Excel, PowerPoint parsers</name>
  <files>scripts/mail_manager/attachment_parser/pdf_parser.py, scripts/mail_manager/attachment_parser/excel_parser.py, scripts/mail_manager/attachment_parser/pptx_parser.py, tests/test_attachment_parser.py</files>
  <behavior>
    - Test 1: PDFParser extracts text from all pages
    - Test 2: ExcelParser handles multiple sheets
    - Test 3: PPTXParser extracts text from all shapes
    - Test 4: Parsers return metadata (page count, sheet names, etc.)
  </behavior>
  <action>
Implement three parsers following RESEARCH.md code examples:

1. pdf_parser.py - PDFParser:
   - can_parse: suffix == ".pdf"
   - extract_text: Use fitz.open(), iterate pages, get_text()
   - extract_metadata: page_count, title if available
   - Close document in finally block

2. excel_parser.py - ExcelParser:
   - can_parse: suffix in (".xlsx", ".xls")
   - extract_text: openpyxl.load_workbook(read_only=True, data_only=True)
   - Iterate all sheets, all rows, join cells with " | "
   - extract_metadata: sheet_names, row_count

3. pptx_parser.py - PPTXParser:
   - can_parse: suffix == ".pptx"
   - extract_text: Presentation(), iterate slides, shapes
   - Extract text from shapes with hasattr(shape, "text")
   - extract_metadata: slide_count

Mock fitz.open, openpyxl.load_workbook, Presentation in tests.
Run tests: pytest tests/test_attachment_parser.py -x
  </action>
  <verify>
    <automated>pytest tests/test_attachment_parser.py -x</automated>
  </verify>
  <done>All three parsers implemented, tests pass with mocked dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement text and markdown parser</name>
  <files>scripts/mail_manager/attachment_parser/text_parser.py, tests/test_attachment_parser.py</files>
  <behavior>
    - Test 1: TextParser handles .txt files
    - Test 2: TextParser handles .md files
    - Test 3: TextParser handles encoding gracefully (utf-8, errors=ignore)
  </behavior>
  <action>
Implement text parser:

```python
class TextParser:
    def can_parse(self, file_path: Path) -> bool:
        return file_path.suffix.lower() in (".txt", ".md")

    def extract_text(self, file_path: Path) -> str:
        with open(file_path, encoding="utf-8", errors="ignore") as f:
            return f.read()

    def extract_metadata(self, file_path: Path) -> dict[str, Any]:
        return {
            "size_bytes": file_path.stat().st_size,
            "type": "text" if file_path.suffix == ".txt" else "markdown"
        }
```

Register in __init__.py.
Run tests: pytest tests/test_attachment_parser.py -x
  </action>
  <verify>
    <automated>pytest tests/test_attachment_parser.py::test_text_parser -x</automated>
  </verify>
  <done>TextParser implemented and registered, tests pass</done>
</task>

</tasks>

<verification>
Run: pytest tests/test_attachment_parser.py -v --cov=scripts/mail_manager/attachment_parser
Expected: All tests pass, coverage > 80%
</verification>

<success_criteria>
- DocumentParser protocol defined
- PDF, Excel, PowerPoint, Text parsers implemented
- Parser factory dispatches by file extension
- All tests pass with mocked dependencies
- parse_attachment() returns ParseResult with text and metadata
</success_criteria>

<output>
After completion, create `.planning/phases/06-smart-enhancements/06-02-SUMMARY.md`
</output>
