---
phase: 06-smart-enhancements
plan: 00
type: execute
wave: 0
depends_on: []
files_modified: [tests/test_llm_client.py, tests/test_thread_manager.py, tests/test_attachment_parser.py, tests/test_reply_assistant.py]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths: []
  artifacts:
    - path: "tests/test_llm_client.py"
      provides: "Test stubs for LLM client abstraction"
      min_lines: 30
    - path: "tests/test_thread_manager.py"
      provides: "Test stubs for thread enhancement"
      min_lines: 30
    - path: "tests/test_attachment_parser.py"
      provides: "Test stubs for document parsing"
      min_lines: 30
    - path: "tests/test_reply_assistant.py"
      provides: "Test stubs for AI reply composition"
      min_lines: 30
  key_links: []
---

<objective>
Create test infrastructure for Phase 6 smart enhancements.

Purpose: Establish TDD foundation with test stubs for all new modules
Output: Test files with failing tests for LLM client, thread manager, attachment parser, reply assistant
</objective>

<execution_context>
@/Users/wuliang/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wuliang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-smart-enhancements/06-RESEARCH.md

## Key Interfaces from Existing Code

From scripts/mail_manager/db.py:
```python
def get_thread_timeline(self, seed_message_id: str, limit: int = 100) -> list[dict]:
    """Get all emails in a thread starting from a seed message."""

def search_emails(self, sender: str | None = None, ...) -> list[dict]:
    """Search emails based on criteria."""
```

From scripts/mail_manager/client.py:
```python
def send_email(self, to: str, subject: str, body: str, ...) -> dict:
    """Send an email via SMTP."""
```

From scripts/mail_manager/detail.py:
```python
def format_email_detail(email: dict, port: int | None = None, ...) -> str:
    """Format complete email detail as Markdown."""
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LLM client test stubs</name>
  <files>tests/test_llm_client.py</files>
  <action>
Create test file with failing tests for LLM client abstraction:

1. Test LLMClient initialization with OpenAI SDK
2. Test chat() method returns LLMResponse with content, model, usage
3. Test chat_with_history() builds message array correctly
4. Test error handling for API failures
5. Test timeout configuration

Use pytest fixtures from conftest.py pattern. Mock openai.OpenAI at library boundary.
All tests should import from `scripts.mail_manager.llm.client` (module does not exist yet - will fail).
  </action>
  <verify>
    <automated>MISSING - Wave 0 creates test scaffold first</automated>
  </verify>
  <done>Test file exists with 5+ test functions, all tests fail with ImportError (module not created yet)</done>
</task>

<task type="auto">
  <name>Task 2: Create thread manager test stubs</name>
  <files>tests/test_thread_manager.py</files>
  <action>
Create test file with failing tests for enhanced thread functionality:

1. Test get_enhanced_thread_timeline() extends existing timeline
2. Test sender/recipient matching for THREAD-03
3. Test thread summary generation via LLM
4. Test format_thread_view() for full/summary display (THREAD-02)
5. Test timeline sorting and deduplication

All tests should import from `scripts.mail_manager.thread_manager` (module does not exist yet).
  </action>
  <verify>
    <automated>MISSING - Wave 0 creates test scaffold first</automated>
  </verify>
  <done>Test file exists with 5+ test functions, all tests fail with ImportError</done>
</task>

<task type="auto">
  <name>Task 3: Create attachment parser test stubs</name>
  <files>tests/test_attachment_parser.py</files>
  <action>
Create test file with failing tests for document parsing:

1. Test PDFParser extracts text via PyMuPDF
2. Test ExcelParser extracts text via openpyxl
3. Test PPTXParser extracts text via python-pptx
4. Test ImageParser calls vision API (ATTACH-AI-02)
5. Test TextParser handles txt/md files
6. Test parser factory dispatches by file extension

Mock fitz.open, openpyxl.load_workbook, pptx.Presentation at library boundaries.
All tests should import from `scripts.mail_manager.attachment_parser` (module does not exist yet).
  </action>
  <verify>
    <automated>MISSING - Wave 0 creates test scaffold first</automated>
  </verify>
  <done>Test file exists with 6+ test functions, all tests fail with ImportError</done>
</task>

<task type="auto">
  <name>Task 4: Create reply assistant test stubs</name>
  <files>tests/test_reply_assistant.py</files>
  <action>
Create test file with failing tests for AI reply composition:

1. Test compose_ai_reply() generates response via LLM
2. Test reply includes thread context when available (REPLY-AI-01)
3. Test user confirmation flow (REPLY-AI-02)
4. Test feedback storage and retrieval (REPLY-AI-03)
5. Test few-shot examples from feedback history

Mock LLMClient for unit tests.
All tests should import from `scripts.mail_manager.reply_assistant` (module does not exist yet).
  </action>
  <verify>
    <automated>MISSING - Wave 0 creates test scaffold first</automated>
  </verify>
  <done>Test file exists with 5+ test functions, all tests fail with ImportError</done>
</task>

</tasks>

<verification>
- All 4 test files created
- Each file has 5+ test functions
- All tests fail with ImportError (modules don't exist yet)
- Test files follow conftest.py fixture patterns
</verification>

<success_criteria>
- tests/test_llm_client.py exists with failing tests
- tests/test_thread_manager.py exists with failing tests
- tests/test_attachment_parser.py exists with failing tests
- tests/test_reply_assistant.py exists with failing tests
- All tests fail with ImportError as expected (TDD red phase)
</success_criteria>

<output>
After completion, create `.planning/phases/06-smart-enhancements/06-00-SUMMARY.md`
</output>
